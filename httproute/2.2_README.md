### 2.2 HTTP 리다이렉트 및 리라이트

HTTPRoute 리소스는 필터를 사용하여 클라이언트에 리다이렉트를 발행하거나 업스트림으로 전송된 경로를 재작성할 수 있습니다.

**참고**: Redirect 및 Rewrite 필터는 상호 배타적입니다. 규칙은 두 필터 유형을 동시에 사용할 수 없습니다.

#### Redirect

리다이렉트는 클라이언트에 HTTP 3XX 응답을 반환하여 다른 리소스를 검색하도록 지시합니다.

##### 지원되는 상태 코드

Gateway API는 다음 HTTP 리다이렉트 상태 코드를 지원합니다:

- **301 (Moved Permanently)**: 리소스가 영구적으로 새 위치로 이동했음을 나타냅니다. HTTP에서 HTTPS로의 영구 업그레이드 또는 영구 URL 변경에 사용합니다.
- **302 (Found)**: 리소스가 일시적으로 다른 위치에서 사용 가능함을 나타냅니다. 상태 코드를 지정하지 않으면 기본값입니다.
- **303 (See Other)**: 요청에 대한 응답을 GET 메서드를 사용하여 다른 URL에서 찾을 수 있음을 나타냅니다. POST 요청 후 확인 페이지로 리다이렉트하는 데 일반적으로 사용됩니다.
- **307 (Temporary Redirect)**: 302와 유사하지만 리다이렉트를 따를 때 HTTP 메서드가 변경되지 않음을 보장합니다.
- **308 (Permanent Redirect)**: 301과 유사하지만 리다이렉트를 따를 때 HTTP 메서드가 변경되지 않음을 보장합니다.

#### HTTP에서 HTTPS로 리다이렉트

HTTP 트래픽을 HTTPS로 리다이렉트하려면 HTTP와 HTTPS 리스너가 모두 있는 Gateway가 필요합니다.

```yaml
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: redirect-gw
  namespace: web
spec:
  addresses:
  - type: "IPAddress"
    value: 192.168.48.202
  gatewayClassName: f5-gateway-class
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: "All"
      kinds:
      - kind: HTTPRoute
  - name: https
    protocol: HTTPS
    port: 443
    allowedRoutes:
      namespaces:
        from: "All"
      kinds:
      - kind: HTTPRoute
    tls:
      mode: Terminate
      certificateRefs:
      - name: local-test-tls-cert
        namespace: sec-infra
        kind: Secret
        group: ""
```

HTTP 리스너에 연결하고 HTTPS로 리다이렉트하는 HTTPRoute가 필요합니다:

```yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: redirect-http-route
  namespace: web
spec:
  parentRefs:
  - name: redirect-gw
    sectionName: http
  hostnames:
  - www.f5bnk.com
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
  - backendRefs:
    - name: httpbin
      port: 8080
```

또한 HTTPS 트래픽을 애플리케이션 백엔드로 전달하는 HTTPS 리스너에 연결된 HTTPRoute도 필요합니다:

```yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: redirect-https-route
  namespace: web
  labels:
    gateway: redirect-gw
spec:
  parentRefs:
  - name: redirect-gw
    sectionName: https
  hostnames:
  - www.f5bnk.com
  rules:
  - backendRefs:
    - name: httpbin
      port: 8080
```

임시로 iRule 기반의 301 Redirect 설정

```yaml
apiVersion: "k8s.f5net.com/v1"
kind: F5BigCneIrule
metadata:
  name: irule-redirect
  namespace: web
spec:
  iRule: >
    when HTTP_REQUEST {
    HTTP::respond 301 Location "https://[getfield [HTTP::host] ":" 1][HTTP::uri]"
    }
```
iRule 설정을 `redirect-gw`에 적용하는 F5 CR(BNKNetPolicy)를 생성 후 해당 Gateway에 적용합니다.

```yaml
apiVersion: "gateway.k8s.f5net.com/v1alpha1"
kind: BNKNetPolicy
metadata:
  name: netpolicy-redirect
  namespace: web
spec:
  extensionRefs:
  - kind: F5BigCneIrule
    name: irule-redirect
    group: k8s.f5net.com
  targetRefs:
  - kind: Gateway
    name: redirect-gw
    group: gateway.networking.k8s.io
    sectionName: http
```
아래와 같이 curl 명령으로 테스트를 진행할 수 있습니다. 또는 브라우저를 통해서도 직접 확인이 가능합니다.

```shell
curl -sk --resolve www.f5bnk.com:80:192.168.48.202 http://www.f5bnk.com/ -vvv -L
(재귀적 질의를 위해 -L 옵션사용)  
```
테스트 결과: 
```shell
(base) ~ » curl -sk -I http://www.f5bnk.com -L -vvv                                                                                                                                   ~ 60 ↵
* Host www.f5bnk.com:80 was resolved.
* IPv6: (none)
* IPv4: 192.168.48.202
*   Trying 192.168.48.202:80...
* Connected to www.f5bnk.com (192.168.48.202) port 80
> HEAD / HTTP/1.1
> Host: www.f5bnk.com
> User-Agent: curl/8.5.0
> Accept: */*
>
* HTTP 1.0, assume close after body
< HTTP/1.0 301 Moved Permanently
HTTP/1.0 301 Moved Permanently
< Location: https://www.f5bnk.com/
Location: https://www.f5bnk.com/
< Server: BigIP
Server: BigIP
* HTTP/1.0 connection set to keep alive
< Connection: Keep-Alive
Connection: Keep-Alive
< Content-Length: 0
Content-Length: 0

<
* Connection #0 to host www.f5bnk.com left intact
* Clear auth, redirects to port from 80 to 443
* Issue another request to this URL: 'https://www.f5bnk.com/'
* Host www.f5bnk.com:443 was resolved.
* IPv6: (none)
* IPv4: 192.168.48.202
*   Trying 192.168.48.202:443...
* Connected to www.f5bnk.com (192.168.48.202) port 443
* ALPN: curl offers h2,http/1.1
* TLSv1.3 (OUT), TLS handshake, Client hello (1):
* TLSv1.3 (IN), TLS handshake, Server hello (2):
* TLSv1.3 (IN), TLS handshake, Encrypted Extensions (8):
* TLSv1.3 (IN), TLS handshake, Certificate (11):
* TLSv1.3 (IN), TLS handshake, CERT verify (15):
* TLSv1.3 (IN), TLS handshake, Finished (20):
* TLSv1.3 (OUT), TLS change cipher, Change cipher spec (1):
* TLSv1.3 (OUT), TLS handshake, Finished (20):
* SSL connection using TLSv1.3 / TLS_AES_128_GCM_SHA256 / X25519 / RSASSA-PSS
* ALPN: server accepted h2
* Server certificate:
*  subject: [NONE]
*  start date: Jan 15 04:36:56 2026 GMT
*  expire date: Apr 15 04:36:56 2026 GMT
*  issuer: CN=f5-bnk-ca
*  SSL certificate verify result: unable to get local issuer certificate (20), continuing anyway.
*   Certificate level 0: Public key type RSA (2048/112 Bits/secBits), signed using sha256WithRSAEncryption
* using HTTP/2
* [HTTP/2] [1] OPENED stream for https://www.f5bnk.com/
* [HTTP/2] [1] [:method: HEAD]
* [HTTP/2] [1] [:scheme: https]
* [HTTP/2] [1] [:authority: www.f5bnk.com]
* [HTTP/2] [1] [:path: /]
* [HTTP/2] [1] [user-agent: curl/8.5.0]
* [HTTP/2] [1] [accept: */*]
> HEAD / HTTP/2
> Host: www.f5bnk.com
> User-Agent: curl/8.5.0
> Accept: */*
>
< HTTP/2 200
HTTP/2 200
< server: gunicorn/19.9.0
server: gunicorn/19.9.0
< date: Mon, 19 Jan 2026 14:10:23 GMT
date: Mon, 19 Jan 2026 14:10:23 GMT
< content-type: text/html; charset=utf-8
content-type: text/html; charset=utf-8
< content-length: 9593
content-length: 9593
< access-control-allow-origin: *
access-control-allow-origin: *
< access-control-allow-credentials: true
access-control-allow-credentials: true
```

`www.f5bnk.com` 호스트이름으로 HTTP Scheme로 접속을 시도하면, 301 Moved Permanently 기반으로 Redirect가 되어 HTTPS Scheme로 접속하는 것을 확인할 수 있습니다. 현재 F5 Gateway API의 경우 2.1 Spec을 기준으로 구현이 되어 있으며 기본 Redirect 동작이라도 iRule 기반의 구현으로 제공합니다. 
